# Состояние проекта — где мы сейчас

> **Живой снимок.** Этот файл всегда отражает текущее положение дел.
> Его не дописывают вниз, а **перезаписывают** — здесь только «сейчас»,
> вся история — в [changelog.md](./changelog.md).
> Обновляй после каждого осмысленного шага и **коммить**.

**Последнее обновление:** —
**Фаза:** Старт

---

## Где мы сейчас

*(2–4 предложения: что уже сделано, на каком этапе проект, что работает.)*

—

## Сейчас в работе

*(Над чем идёт работа прямо в этот момент. Если ничего — «—».)*

—

## Следующие шаги

*(Приоритизированный список. Первый пункт — то, за что берётся следующий исполнитель.)*

- [ ] Раскатить `www/` на хостинг (`bash deploy-prod.sh`)
- [ ] Открыть `init.php` в браузере — создаст `www/env.php`
- [ ] Создать local-app на портале Б24, вписать CLIENT_ID / SECRET в `www/env.php`
- [ ] Открыть приложение из меню Б24 — двухфазный install-flow отработает сам
- [ ] Удалить `www/init.php` через файловый менеджер хостинга

## Открытые вопросы / блокеры

*(Что мешает двигаться или требует уточнения у заказчика.)*

—

## Карта проекта

| Что | Где |
|-----|-----|
| Точка входа (handler) | `www/index.php` |
| OAuth, токены | `www/api/b24.php` |
| Файловый store | `www/api/store.php` |
| Плейсменты | `www/api/bind.php` |
| Admin-gate | `www/api/session.php` |
| UI-шаблон | `www/template.html` |
| JS-фронтенд | `www/js/app.js` |
| Cron-задача | `www/bin/process.php` |
| Install-flow диаграмма | `docs/install-flow-diagram.md` |
| Прод URL | — |
| Хостинг / slug | Netangels, slug: — |
