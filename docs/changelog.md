# История изменений

> Хронология того, **что уже сделано** — новое сверху.
> Каждая запись = один осмысленный шаг (фича, фикс, деплой, решение).
> Файл **только дописывается**. Подробности любого шага всегда можно
> увидеть в git: `git log --oneline`, `git show <hash>`.
>
> Пиши коротко: «что изменилось» и «зачем», а не «изменён файл X».

## Формат записи

```
## ГГГГ-ММ-ДД — Заголовок шага
- Что сделали (по пунктам)
- Зачем / что это даёт
- Связанные решения: D-NNN (если есть)
```

---

<!-- Новые записи добавляй СВЕРХУ, сразу под этой строкой -->

## 2026-06-30 — 4 прототипа вкладки ЗОВ (только фронт)
- Изучена вся документация по смарт-процессу ЗОВ (#1036): поля, стадии, подзадачи (список #75), лимиты (#76), ИД (#77), аккредитация
- Создано 4 standalone HTML-прототипа в `prototypes/` с mock-данными:
  - `v1-dashboard.html` — сетка KPI + команда + подзадачи на одном экране
  - `v2-sections.html` — герой-баннер + вертикальные секции, читается как журнал
  - `v3-stageflow.html` — визуальная цепочка стадий + 2-колоночный layout
  - `v4-tabbed.html` — тёмная шапка с KPI + вкладки (SPA-стиль)
- Бэкенд не разрабатывался — сначала согласование UI
- Связанные решения: D-002

## — — Старт проекта
- Создан проект из шаблона `_b24-single-php`
- Подключена система журналирования: `docs/{handoff,state,decisions,changelog}.md`
- Дальше: деплой `www/` → `init.php` → создание local-app в Б24
